name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "practice-app/**"
      - "docs-site/**"
      - ".github/workflows/deploy-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Build practice app
        run: cd practice-app && pnpm build

      - name: Build docs site
        run: cd docs-site && pnpm build

      # Docs site (VitePress) is the primary front door — served at the Pages
      # root so the README's documentation links resolve. The practice app
      # (single-file build, HashRouter) is served under /app.
      - name: Combine artifacts
        run: |
          mkdir -p _site
          cp -r docs-site/dist/. _site/
          mkdir -p _site/app
          cp -r practice-app/dist/. _site/app/

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
